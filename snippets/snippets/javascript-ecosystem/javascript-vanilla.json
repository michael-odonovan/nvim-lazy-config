{
  "javascript function vanilla": {
    "prefix": "fun",
    "body": [
      "function ${1}(${2}) {",
      "  ${3}",
      "}"
    ],
    "description": ""
  },
  ".map()": {
    "prefix": "map",
    "body": [
      "map((${1}) => (",
      "  <li key={${1}.${2}}>",
      "    {${1}.${3}}",
      "  </li>",
      "))",
      ""
    ],
    "description": ".map()"
  },
  ".filter()": {
    "prefix": "filter",
    "body": [
      "filter((${1}) => ${2} > ${3});",
      "",
      ""
    ],
    "description": ".filter()"
  },
  "switch": {
    "prefix": "switch",
    "body": [
      "switch(${1}) {",
      "  case ${2}:",
      "    ${3};",
      "    break;",
      "  case ${4}:",
      "    ${5};",
      "    break;",
      "  default:",
      "    ${6}",
      "} ",
      "",
      ""
    ],
    "description": "switch"
  },
  "for loop": {
    "prefix": "for loop",
    "body": [
      "for (let i = ${1}; i < ${2}; i++) {",
      "  ${3:body};",
      "} "
    ],
    "description": "for loop"
  }
}
